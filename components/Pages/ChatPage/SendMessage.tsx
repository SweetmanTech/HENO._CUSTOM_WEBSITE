const SendMessage = () => (
  <div className="flex gap-2">
    <input type="text" className="grow bg-black focus:!ring-0 !border-[white] rounded-md" />
    <button type="button" className="border px-3 rounded-md">
      Send
    </button>
  </div>
)

export default SendMessage
